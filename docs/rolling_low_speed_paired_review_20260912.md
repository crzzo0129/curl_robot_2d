同初态复评确认：第一段低速 DAgger 续训减轻了高速直行侧漂，但没有达到改善低速速度跟踪的目标。下一步停止这套 DAgger 续训配方，沿用原校准蒸馏策略进行 critic 预热，再小步 PPO。保留候选权重和诊断，不以总体成功率最高作为唯一选择依据。

数据来自更新后的 `rolling_low_speed_20260912_065438_diagnostics.zip` 中 `recheck_20260912_075121/`。本地提取目录为 `results/rolling_low_speed_recheck_review_20260912/rolling_low_speed_20260912_065438/recheck_20260912_075121/`；额外计算的分组配对统计保存在其中的 `paired_analysis.json`。本次仅分析文件，没有运行本地测试、训练或仿真。

两组 `initial_state_sha256` 均为 `adc759012430ddf1b60f8949a1e013a79b27ef91db6669468494721b7bb70cc0`。基线日志确认生成缓存，候选日志确认直接加载缓存；256 条命令和预热步数逐条相同，首帧 heading、Y、vy、time 差异均为零。同状态教师标签首帧仍有约 1.26e-6 的数值差异，说明同初态并不保证不同进程的所有设备计算逐位一致。本轮足以作配对比较，但仍只有一个 256 环境面板。

| 指标 | 原策略 | dagger_01 |
|---|---:|---:|
| 总体成功 | 180/256，70.3% | 192/256，75.0% |
| 最低速度补充判据通过 | 69.1% | 73.8% |
| 跑满 10 秒 | 229/256，89.5% | 249/256，97.3% |
| 侧漂终止 | 27 | 7 |
| 总体 vx MAE，m/s | 0.16048 | 0.16642 |
| 总体 yaw 逐步 MAE，rad/s | 0.04999 | 0.04737 |
| 低速成功 | 42/91，46.2% | 36/91，39.6% |
| 低速 vx MAE，m/s | 0.22682 | 0.23935 |
| 中速成功 | 76/78，97.4% | 75/78，96.2% |
| 高速成功 | 62/87，71.3% | 81/87，93.1% |
| 高速直行成功 | 12/37，32.4% | 31/37，83.8% |

逐条配对：总体新增成功 24 条、丢失成功 12 条，净增 12 条。低速新增成功 0 条、丢失 6 条；中速新增 2 条、丢失 3 条；高速新增 22 条、丢失 3 条。高速组转向轨迹两策略都成功，变化集中在直行。可确认高速侧漂改善，并非之前命令面板变化造成的全部假象；但之前的 79.7% 不能继续用作这组同状态比较的候选成绩。

低速 91 条两策略均跑满时长。新增 6 条未成功是有效滚动进展不足，不是新增机械失败。低速直行成功 24→22，左转 12→11，右转 6→3；右转组 vx MAE 从 0.29132 增至 0.33862 m/s，尤其需要关注。

轨迹净平均 yaw MAE（先对每条轨迹积分，再取绝对误差）总体为 0.009665→0.009322 rad/s，低速为 0.016414→0.017926，中速为 0.004932→0.004586，高速为 0.006850→0.004567。两组各有 14 条净转弯方向错误，全部来自低速。候选保有中高速转向能力，但低速转向没有同步改善；逐步 yaw MAE 的下降不能替代这些分组结论。

在各自访问状态上的教师标签误差：整体共模 RMSE 0.02348→0.03656，低速共模 RMSE 0.03277→0.05546；整体差动 RMSE 0.00364→0.00493。这是闭环偏离加重的相关证据，不能单独确定相位估计、教师状态覆盖或优化器哪一项是根因。包内没有独立教师闭环轨迹，不能据此证明教师已达到性能上限。

筛选器本次按真实指标拒绝候选：总体 vx MAE 增加 0.00594 m/s，超过原定 0.005 上限，且低速成功率和低速速度误差都未改善。`comparison.json` 中 `selection_applied=true`，运行根目录 `recovery.json` 的 `selected_student` 已保留原 `rolling_distill_calibrated_20260912_034719/student_params`。不需要手工改路径或恢复旧权重。

现在可在云端项目根目录运行已有 critic 入口，不必重新训练这 500 次 DAgger，也无需再改源码：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 python -u \
  -m scripts.run_rolling_low_speed_recovery critic \
  --out results/rolling_low_speed_20260912_065438
```

入口从运行清单读取原策略，沿用校准表、完整命令范围、DR=0；冻结 actor，先训练 critic 409600 环境步。actor 参数差应严格为零，固定面板评价应基本稳定。PPO 快照池的筛选和评价入口不同于蒸馏，需以 PPO 自己 step=0 的固定结果作为训练基线，不能直接把其成功率与上表混合。

critic 完成后，查看并回传更新的 `rolling_low_speed_20260912_065438_diagnostics.zip` 或目录内 `critic_diagnostics.zip`。确认 critic 和 actor 冻结检查正常后，再使用已准备的 actor 阶段（初始及最大学习率 3e-6）。RL 是否能够改善低速和漂移仍需云端结果验证；本轮证据支持改变优化目标，暂不继续增加同一 DAgger 配方的步数或开启 DR。
