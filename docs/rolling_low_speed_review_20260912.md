本轮第一段补训得到一个值得保留复评的候选，但尚不能确认低速问题改善。筛选器停止的直接原因是评价命令面板改变，不是已经判定策略退化。种子隔离问题来自我编写的 v1 续训入口，已在 v2 修复；这 500 次更新不用重跑。

数据来自 `rolling_low_speed_20260912_065438_diagnostics.zip`，本地解包到 `results/rolling_low_speed_review_20260912/rolling_low_speed_20260912_065438/`。只读取文件并计算已有轨迹统计，没有运行本地训练、仿真或测试。

| 指标 | 原策略 baseline | 补训 dagger_01 |
|---|---:|---:|
| 成功 | 180/256，70.3% | 204/256，79.7% |
| 跑满 10 秒 | 89.5% | 98.4% |
| 侧漂终止（全部正 Y） | 27 | 4 |
| vx MAE，m/s | 0.16028 | 0.14961 |
| yaw 逐步 MAE，rad/s | 0.05007 | 0.05020 |
| 低速成功 | 43/91，47.3% | 35/81，43.2% |
| 低速 vx MAE，m/s | 0.22622 | 0.20130 |
| 高速直行成功 | 11/37，29.7% | 18/22，81.8% |

这些是不同命令面板上的观测值，不能视为严格的配对提升。补训组低速和高速直行样本更少，左右转命令构成也变了：直行 113→91、右转 70→92。总体成功率增加部分可能来自面板难度变化。

两组 `eval_seed=20260916`，保存的 `eval_reset_keys` 也完全相同，teacher/direct task 配置一致；但 256 条 vx 命令全部不同，219 条 yaw 命令不同，预热步数相同。v1 恢复入口令候选训练 seed 从 20260912 变为 20260913，而环境 `reset` 内部执行 `fold_in(rng, self.seed)`。评价 CEM 快照仍借用训练环境的 reset，因而改变了实际命令与初态。自动筛选在 `Evaluation commands or warmup changed` 处退出，`selected_student` 正确保留原策略，并未丢失新权重。

训练配置实际生效：500 次更新、学习率 2e-5、教师干预约 10%→0%，首批速度组采样为 1231/413/404，即 60.1%/20.2%/19.7%；2048 次抽样对应 1187 个不同快照。第二池有 1196 个不同快照。聚焦模式的接管后 10 秒预算生效，最后一步重置比例 94.7% 主要是同步到达 500 步超时，不能当作机器人失败率。DAgger 阶段日志耗时 414.6 秒，包含编译及池刷新。

从轨迹计算的净平均 yaw 显示候选仍有转向能力：左转目标均值 +0.04650、实际 +0.03536 rad/s；右转目标 -0.04789、实际 -0.03871 rad/s。反向净转弯 5 条，全部低速。高速直行平均净 yaw 从基线面板的 +0.00615 降至候选面板的 +0.00091 rad/s，有减轻系统性偏航的迹象，但仍需同状态复评确认。

候选低速 81 条全部跑满时长，46 条因有效进展不足而未成功。共模动作误差整体从 0.02404 到 0.02961，低速从 0.03385 到 0.04447；差动误差整体从 0.00364 到 0.00479。动作标签来自学生状态副本上的教师查询，不能用这些差异单独断言教师或学生谁导致漂移；低速问题尚未解决。

v2 修复包括独立的评价 reset salt、与训练循环隔离的评价噪声/标签随机数，以及持久化实际状态/历史/上一动作的 NPZ。基线生成一次，候选直接加载，报告同一 `initial_state_sha256`；命令一致和物理状态校验值一致均是比较检查项。新 `recheck` 阶段只评价原策略和已保存候选，仍沿用原筛选阈值，不为接受当前候选而放宽规则。现有权重不改动，旧结果也保留。

下一步在云端按 `docs/rolling_low_speed_recovery.md` 的 recheck 命令运行，取得可比结果后再决定所选学生是否进入 critic/PPO。v2 本地验证仅限语法、参数声明与打包完整性的静态核对；新增轻量单元检查供云端运行，尚未在本地执行。
