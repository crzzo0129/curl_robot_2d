# Calibrated distillation review: cloud run 20260912_034719

Input: user-provided `distillation.json`, 297223 bytes. Reviewed JSON and source only; no new training or simulation during this review.

## Confirmed configuration

Both teacher_task and closed_loop_task contain `assets/controllers/rollingquad_abd10_high_speed_steering_v1.json`. Geometry is rollingquad_2_abd10_no_self_collision; cg20, 50 Hz, four GPUs, 2048 training environments, 256 evaluation environments. The reference is the intended high-speed zero-contact refinement. BC=2000, DAgger=1000; evaluation seed 20260916; warmup 100..300 steps. Deployment DR is disabled and nominal closed-loop observation noise is zero. The JSON records the calibration path, not the asset's full contents or hash; no run-time source bundle was supplied.

## Failure accounting

181/256 success, 230/256 full-horizon and failure-free. The 26 failures are all lateral drift: 25 high-speed straight and one medium-speed straight. No axis-tilt, root-height, collision, or nonfinite failure is reported. Mean failure time is 7.113 s after takeover (range 3.62..9.86 s).

49 episodes survive 10 seconds but miss the five-effective-turn criterion: 48 low-speed episodes and one medium-left episode. Low-speed episodes all survive. The minimum-speed criterion requires 5.13165 effective turns, so replacing five turns with that criterion does not resolve this shortfall.

| Group | n | Success | Lateral failures | Mean effective turns |
|---|---:|---:|---:|---:|
| Low straight | 39 | 25 | 0 | 3.960 |
| Low left | 28 | 12 | 0 | 3.618 |
| Low right | 24 | 6 | 0 | 2.414 |
| Medium straight | 37 | 36 | 1 | 7.842 |
| Medium left | 21 | 20 | 0 | 6.630 |
| Medium right | 20 | 20 | 0 | 6.822 |
| High straight | 37 | 12 | 25 | 7.851 |
| High left | 24 | 24 | 0 | 8.520 |
| High right | 26 | 26 | 0 | 9.148 |

26 episodes have negative effective progress (11 straight, seven left, eight right), and 33 have fewer than one effective turn. For forward commands below .46 m/s, only one of 30 episodes succeeds. This is not merely a narrowly missed success threshold. Effective progress uses conservative rotation/world-X translation increments, so it is not a heading-relative traveled-distance metric. The straight episodes with negative progress also rule out attributing all low-speed shortcomings to turn projection alone. Actual stall/reversal timing needs trajectories.

## Learning curves and missing evidence

BC action RMSE decreases from .52335 to .0118895, with loss .000299828 at step 2000. DAgger action RMSE is .00741 at its first step on the initial teacher-state distribution, rises to .06421 at step 100 on student-visited states, then decreases to .016738 at step 1000. Last recorded maximum absolute action error is .236908. These diagnostics show learning and no numeric explosion; they do not establish closed-loop convergence or a benefit/harm from DAgger. BC and DAgger errors are on different state distributions. `loss_history` contains 21 BC and 11 DAgger records; `dagger_loss_history` repeats the latter, not another training stage.

Final `velocity_rmse=.0484` measures the auxiliary velocity estimator, not command tracking. Actual world-X per-step tracking MAE is .160325 m/s.

The JSON contains only absolute tracking-error sums. It does not contain signed velocity sums, actual heading trajectories, or an independent teacher rollout. `record_diagnostics=false`. Therefore it cannot establish whether the student's high yaw MAE is primarily mean-rate bias, wrong direction, or oscillation. The CPU calibration's settled-mean yaw error and the student's stepwise yaw MAE are different metrics. The attachment is sufficient to locate failure groups, but insufficient to assign the turning deficit to teacher transfer or student imitation.

## Next bounded check

Use `scripts/diagnose_rolling_distillation.py` to evaluate both existing `student_params_before_dagger` and `student_params`, preserving the saved settings and enabling `--record-diagnostics`. It does not train or update checkpoints. It collects heading trajectories and same-state teacher/student common and differential action errors into one ZIP. Teacher queries are action labels at student states, not independent teacher rollouts. Seeds/settings match but snapshots are regenerated by separate processes, so do not claim exact bitwise pairing.

Cloud command after syncing `results/rolling_distill_review_v1.zip` through Git:

```bash
unzip -o results/rolling_distill_review_v1.zip
CUDA_VISIBLE_DEVICES=0,1,2,3 python -u -m scripts.diagnose_rolling_distillation \
  results/rolling_distill_calibrated_20260912_034719
```

The wrapper saves a timestamped `results/rolling_distill_review_*_diagnostics.zip`. If BC already has the low-speed issue, investigate coverage/teacher handoff and same-state imitation before adding RL. If BC is healthy and DAgger loses it, investigate DAgger updates and reset distribution. If heading averages track commands despite high stepwise error, treat oscillation and average tracking separately. Continue to preserve the old baseline; do not launch longer distillation, PPO, or DR solely from these summary losses.
